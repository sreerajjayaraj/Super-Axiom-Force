# Changelog - Super Axiom Force v1.12.2 / Crimson Desert 1.0.0.1855 config-r4

- Restored live INI option handling after the game update pass.
- Fixed UTF-8 BOM INI parsing so `[AxiomForce]` settings are read correctly.
- Confirmed `StraightPull=false` and custom `Range` / `PullSpeed` values are parsed instead of silently falling back to defaults.
- Preserved Instant Axiom, range extension, pull speed, and straight-pull feature layout.
- Confirmed in-game by user playtest: Super Axiom Force works with the updated build.